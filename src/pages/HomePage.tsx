import { motion } from 'framer-motion'
import { Button } from '@/components/ui/but
import { Button } from '@/components/ui/button'
import { Phone, Envelope, Star, Clock, TrendUp } from '@phosphor-icons/react'
interface HomePageProps {


  const greenLe
  const redLeads = leads.filter(lead
 

  return (
      <motion.div
        animate={{ opacity: 1, y: 0 }}
        className="text-center"
  
          style={{
            borderRadius: '2rem',
          }}

          
            style={{
            }}
        </div>
        <h1 className="text-5xl md:tex
        </h1>
          AI Operating System f
      <
      <div cl
          onClick={() => onNavigate('le
          whileTap
          animate={{ opacity
          className="glass-card p
          <div className="text-5xl font-bold text-success mb-2">{greenLeads.length}</div>
        </mo
        <
          while
          initial={{ opacity: 0
          transition={{ delay: 0.
        >
          <div class

          onCl
          wh
          anim
        
          <div className="text-5xl font-bold text-danger mb-2">{redLead
        </motion.button>

        <motion.div
          animate={{ opacity: 1, x: 0 }}
        >
            <div cl

              <Button
                classN
              >
                View Dashboard
              <Button
                className="w-full bg-succ
              >
              </Button>
          </Card>

          initial={{ opacity: 0, x: 20 }}
          transition={{ delay: 0.5 }}
          <Card classNam

            </div>
              <div className="text-center py-
              </div>
              <div className="space-
                  <motion.div
                    initial={{ opacity: 
                    transition={{ del
                    style={{
         
                  >
                      <div className="flex-1">
                        

                      
                          <span className="te
                          </span>
                      </div>
                    
                      <a
                        className="fl
                          background: 'var(--primary)',
         
                        <Phone size={16} weight="duotone" />
                      <a
                        
            

                        <Envelope size={16} weight="duotone" 
                   
                ))}
            )}
        </motion.div>
    </div
}



































































































